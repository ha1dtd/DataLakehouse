# Dashboard Widgets Domain

This domain holds the HDOS widget-oriented pipeline assets for the add-on package.

Expected contents:

- `dags/`: Airflow entrypoints
- `jobs/`: executable Spark or helper jobs
- `config/`: default package config
- `sql/`: optional SQL assets
- `services/`: reserved for later phases
